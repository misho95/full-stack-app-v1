import styled from "styled-components";

const LoginPage = () => {
  return <MainComponent>Login Page</MainComponent>;
};

const MainComponent = styled.main`
  display: flex;
  min-width: 100vw;
  min-height: 100vh;
`;

export default LoginPage;
